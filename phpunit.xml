<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="PostgreSQL Integration">
            <directory>tests/Integration</directory>
        </testsuite>
        <testsuite name="Modules">
            <directory>app/Modules/*/Tests</directory>
            <directory>app/Modules/*/*/Tests</directory>
        </testsuite>
        <testsuite name="Extensions">
            <directory>extensions/*/*/Tests</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <!-- cache + array store keeps maintenance mode process-local. The file
             driver wrote storage/framework/down, which the LIVE dev server also
             reads: deployment tests calling artisan down took the real site
             offline for ~35s per run (verified 2026-07-12). -->
        <env name="APP_MAINTENANCE_DRIVER" value="cache"/>
        <!-- Codex model discovery auto-resolves client_version from the GitHub
             releases API; tests must never make that live call. Resolver tests
             re-enable it against Http::fake. -->
        <env name="AI_CODEX_AUTO_CLIENT_VERSION" value="false"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="ARGON_MEMORY" value="1024"/>
        <env name="ARGON_TIME" value="1"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Point tests away from the live FrankenPHP admin API (2020/2019):
             worker-reload code paths are Http::faked in tests, but an escaped
             real call must never be able to restart the dev server's workers. -->
        <env name="CADDY_SERVER_ADMIN_HOST" value="127.0.0.1"/>
        <env name="CADDY_SERVER_ADMIN_PORT" value="2098"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
