{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "dev:all": "bunx concurrently -c \"#93c5fd,#c4b5fd,#fcd34d,#86efac\" \"php artisan octane:start --server=frankenphp --caddyfile=Caddyfile --host=${APP_BIND_HOST:-127.0.0.1} --port=${APP_PORT:-8000} --admin-port=${CADDY_SERVER_ADMIN_PORT:-2019}\" \"php artisan queue:work --queue=ai-chat-turns,ai-agent-tasks,ai-background-commands,ai-schedules,default --tries=1 --timeout=900 --sleep=1\" \"php artisan schedule:work\" \"bun run dev\" --names=server,queue,scheduler,vite",
        "dev:all:watch": "bunx concurrently -c \"#93c5fd,#c4b5fd,#fcd34d,#86efac\" \"php artisan octane:start --server=frankenphp --caddyfile=Caddyfile --host=${APP_BIND_HOST:-127.0.0.1} --port=${APP_PORT:-8000} --admin-port=${CADDY_SERVER_ADMIN_PORT:-2019} --watch\" \"php artisan queue:work --queue=ai-chat-turns,ai-agent-tasks,ai-background-commands,ai-schedules,default --tries=1 --timeout=900 --sleep=1\" \"php artisan schedule:work\" \"bun run dev\" --names=server,queue,scheduler,vite"
    },
    "dependencies": {
        "@tailwindcss/vite": "^4.3.3",
        "alpinejs": "^3.15.12",
        "autoprefixer": "^10.5.4",
        "axios": "^1.19.0",
        "concurrently": "^10.0.4",
        "laravel-vite-plugin": "^3.1.3",
        "playwright": "^1.62.1",
        "sortablejs": "^1.15.7",
        "tailwindcss": "^4.3.3",
        "vite": "^8.2.1"
    },
    "optionalDependencies": {
        "@rollup/rollup-linux-x64-gnu": "4.62.4",
        "@tailwindcss/oxide-linux-x64-gnu": "^4.3.3",
        "lightningcss-linux-x64-gnu": "^1.33.0"
    },
    "overrides": {
        "nanoid": "^3.3.17",
        "postcss": "^8.5.23",
        "shell-quote": "^1.10.0"
    }
}
